listview :修改为 40dp
显示加载框的地方是：NetworkUtils.checkNet 
关闭加载框的地方：请求网络的回调 StringCallback  onResponse
	  public void onResponse(String response, int id) {
        Log.i("StringCallback", "response=" + response);
        CustomProgressDialog dialog = CustomProgressDialog.getCustomProgressDialog() ;
        if (dialog != null && dialog.isShowing()){
            dialog.dismiss();
        }
    }


解决没有加载框的方案：
 重写基类人脸识别成功的回调 showSuccessInfoFile(String response)  修改里边加载框的逻辑
 如果加载框不为null 没有显示的话直接显示，不调用  dialog.dismiss();的方法

showFailInfoFile(String response)的逻辑暂时不用怎么修改

按钮不能点击的逻辑

人脸识别成功以后，将按钮置为不可点击，
提交成功或者提交订单失败后再将按钮置为可点击






